% Facts
fact(sunny).
fact(warm).

% Rules
rule(sunny, outdoor).
rule(warm, comfortable).
rule(outdoor, happy).
rule(comfortable, happy).

% Forward chaining
derive(X) :-
    fact(X).

derive(X) :-
    rule(Y, X),
    derive(Y).
